# Test override: jwk-set-uri is fetched lazily so the context starts without a live IdP.
spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://127.0.0.1:9000/oauth2/jwks
