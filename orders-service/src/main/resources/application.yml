server:
  port: 8081

spring:
  application:
    name: orders-service
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${IDP_ISSUER_URI:http://127.0.0.1:9000}

management:
  endpoints:
    web:
      exposure:
        include: health
